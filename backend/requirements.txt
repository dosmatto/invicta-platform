# Dependencias PINADAS (versoes exatas) — build reproduzivel e imune a um
# "latest" futuro mais lento (a lentidao da interpolacao ja foi suspeita de
# atualizacao de libs; pinar elimina esse risco). Estas sao as versoes
# CONFIRMADAS funcionando (jul/2026) na nuvem (Render) e no interpolador local.
# Para atualizar de proposito: suba as versoes aqui e rode o backend p/ validar.
fastapi==0.140.0
uvicorn[standard]==0.51.0
gunicorn==26.0.0
uvicorn-worker==0.4.0
numpy==2.5.1
scipy==1.18.0
pykrige==1.7.3
shapely==2.1.2
pillow==12.3.0
rasterio==1.5.0
pystac-client==0.9.0
