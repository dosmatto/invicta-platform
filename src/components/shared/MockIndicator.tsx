export function MockIndicator() {
  return (
    <span className="inline-flex items-center gap-1 px-1.5 py-0.5 rounded text-[10px] font-medium"
      style={{ background: 'var(--mock-bg)', color: 'var(--mock-color)' }}>
      SIMULADO
    </span>
  );
}
