<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- GPS: o app de campo navega até o ponto e só libera a coleta dentro do
         raio permitido, então precisa da localização PRECISA (fina). A COARSE
         entra junto porque alguns aparelhos negociam a fina a partir dela. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Câmera: fotos do ponto de coleta (amostra, paisagem, problema). -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- SEM permissão de leitura da galeria (READ_MEDIA_IMAGES /
         READ_EXTERNAL_STORAGE), de propósito. A única entrada de imagem do app
         é o <input type="file" accept="image/*" capture="environment"> da tela
         de coleta, que abre a CÂMERA direto — a galeria nunca é lida. Declarar
         acesso a toda a galeria para um app que só tira foto é justamente o que
         a política "Usar seletores de sistema alternativos para fotos/vídeos"
         proíbe: ela barrou o envio da 2.136.0 na verificação da Play Store.
         Se um dia for preciso escolher uma imagem já existente, use o seletor
         do sistema (Android Photo Picker), que também dispensa permissão. -->

    <!-- Estado da rede: o app mostra online/offline e dispara a sincronização
         quando a internet volta. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Mantém a tela ligada durante o trabalho em campo (wake lock). -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- GPS é essencial para este app, mas não impedimos a instalação em
         aparelho sem ele (required=false) — a tela avisa se faltar sinal. -->
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
</manifest>
